node_modules
.DS_Store
.vscode/
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

src/test/a11y/.user-auth.json
src/test/a11y/.admin-auth.json
src/test/a11y/.fixtures.json
playwright-report/
test-results/
.playwright-mcp/

# Subagent-driven-development scratch: ledgers, briefs, review packages.
.superpowers/

# Personal Claude Code overrides. `.claude/settings.json` is tracked and shared;
# this one is not, and the directory being tracked makes it committable by accident.
.claude/settings.local.json
